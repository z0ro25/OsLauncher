<?xml version="1.0" encoding="utf-8"?>
<!-- Outline + viền cho popup dropdown nút Edit. Fill trong suốt (blur + tint do RealtimeBlurView
     trong edit_home_menu lo); shape này tạo outline bo góc cho clipToOutline + vẽ viền kính sáng
     1dp khi dùng làm foreground, cho ra chất kính giống hotseat. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <corners android:radius="@dimen/bg_round_rect_radius"/>
    <solid android:color="#00FFFFFF"/>
    <stroke
        android:width="@dimen/dock_glass_stroke_width"
        android:color="@color/dock_glass_stroke_color"/>
</shape>
