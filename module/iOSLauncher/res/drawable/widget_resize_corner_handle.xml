<?xml version="1.0" encoding="utf-8"?>
<!-- Tay nắm resize GÓC DƯỚI-PHẢI cho chế độ resize gọn (long-press widget).
     Hình dấu GÓC VUÔNG (┘) ôm góc dưới-phải. FILE RIÊNG, không đụng ic_widget_resize_handle
     (vốn dùng cho 4 tay kéo cạnh của luồng resize cũ). -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="22dp"
    android:height="22dp"
    android:viewportWidth="24"
    android:viewportHeight="24">

    <!-- Nét viền tối mờ phía sau để icon nổi trên nền widget sáng -->
    <path
        android:strokeColor="#66000000"
        android:strokeWidth="4"
        android:strokeLineCap="round"
        android:strokeLineJoin="round"
        android:fillColor="#00000000"
        android:pathData="M18,7 L18,18 L7,18" />

    <!-- Nét trắng chính -->
    <path
        android:strokeColor="#FFFFFFFF"
        android:strokeWidth="2.4"
        android:strokeLineCap="round"
        android:strokeLineJoin="round"
        android:fillColor="#00000000"
        android:pathData="M18,7 L18,18 L7,18" />
</vector>
