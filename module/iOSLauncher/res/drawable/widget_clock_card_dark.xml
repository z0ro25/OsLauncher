<?xml version="1.0" encoding="utf-8"?>
<!-- Nền thẻ TỐI cho widget đồng hồ (Clock I, World Clock, World Clock 2x2). Prefix
     widget_clock_ để KHÔNG dùng chung với drawable khác (scope no side-effects). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#FF1C1C1E" />
    <corners android:radius="@dimen/widget_round_corner" />
</shape>
