<resources>
    <!-- Dynamic Grid -->
    <!-- Out of 100, the percent of space the overview bar should try and take vertically. -->
    <integer name="config_dynamic_grid_overview_icon_zone_percentage">20</integer>

    <!-- Miscellaneous -->
    <bool name="allow_rotation">false</bool>

    <!-- Max number of page indicators to show -->
    <integer name="config_maxNumberOfPageIndicatorsToShow">21</integer>

    <!-- App data backup and restore. To enble backup, register with an android backup service.
         http://developer.android.com/guide/topics/data/backup.html#BackupKey -->
    <bool name="enable_backup">false</bool>

    <!-- DragController -->
    <integer name="config_flingToDeleteMinVelocity">-1500</integer>
    <item name="drag_event_parity" type="id" />

    <!-- AllApps & Launcher transitions -->
    <!-- The alpha of the AppsCustomize bg in spring loaded mode -->
    <integer name="config_workspaceScrimAlpha">40</integer>
    <integer name="config_allAppsTransitionTime">100</integer>
    <integer name="config_overviewTransitionTime">250</integer>

    <!-- Out of 100, the percent to shrink the workspace during spring loaded mode. -->
    <integer name="config_workspaceSpringLoadShrinkPercentage">85</integer>
    <!-- Out of 100, the percent to shrink the workspace during overview mode. -->
    <integer name="config_workspaceOverviewShrinkPercentage">70</integer>
    <!-- Out of 100, the percent to shrink the workspace root view during overview mode. -->
    <integer name="config_workspaceRootViewShrinkPercentage">85</integer>

    <!-- Fade/zoom in/out duration & scale in a Launcher overla y transition.
         Note: This should be less than the config_overlayTransitionTime as they happen together. -->
    <integer name="config_overlayRevealTime">220</integer>
    <integer name="config_overlayTransitionTime">300</integer>
    <integer name="config_overlayItemsAlphaStagger">60</integer>

    <!-- This constant stores the ratio of the all apps button drawable which
         is used for internal (baked-in) padding -->
    <integer name="config_allAppsButtonPaddingPercent">17</integer>

    <integer name="config_workspaceDefaultScreen">0</integer>

    <!-- Workspace -->
    <!-- The duration (in ms) of the fade animation on the object outlines, used when
         we are dragging objects around on the home screen. -->
    <integer name="config_dragOutlineFadeTime">900</integer>

    <!-- The alpha value at which to show the most recent drop visualization outline. -->
    <integer name="config_dragOutlineMaxAlpha">128</integer>

    <!-- Parameters controlling the animation for when an item is dropped on the home screen,
         and it animates from its old position to the new one. -->
    <integer name="config_dropAnimMinDuration">100</integer>
    <integer name="config_dropAnimMaxDuration">500</integer>

    <integer name="config_dropTargetBarTime">600</integer>
    <integer name="config_dropTargetBgTransitionDuration">150</integer>
    <integer name="config_dropTargetDelayTime">300</integer>

    <!-- The duration of the UserFolder opening and closing animation -->
    <integer name="config_folderExpandDuration">200</integer>

    <!-- The distance at which the animation should take the max duration -->
    <integer name="config_dropAnimMaxDist">800</integer>

    <!-- Hotseat -->
    <bool name="hotseat_transpose_layout_with_orientation">true</bool>

    <!-- Memory debugging, including a memory dump icon -->
    <bool name="debug_memory_enabled">false</bool>

    <!-- Name of a subclass of com.amz.ios.launcher.AppFilter used to
         filter the activities shown in the launcher. Can be empty. -->
    <string name="app_filter_class" translatable="false"></string>

    <!-- Name of a subclass of com.amz.ios.launcher.BuildInfo used to
         get build information. Can be empty. -->
    <string name="build_info_class" translatable="false"></string>

    <!-- View ID to use for QSB widget -->
    <item name="qsb_widget" type="id" />

    <!-- Accessibility actions -->
    <item name="action_remove" type="id" />
    <item name="action_uninstall" type="id" />
    <item name="action_info" type="id" />
    <item name="action_add_to_workspace" type="id" />
    <item name="action_move" type="id" />
    <item name="action_move_to_workspace" type="id" />
    <item name="action_move_screen_backwards" type="id" />
    <item name="action_move_screen_forwards" type="id" />
    <item name="action_resize" type="id" />
    <item type="id" name="action_deep_shortcuts" />
    <item type="id" name="action_dismiss_notification" />

    <!-- Popup items -->
    <integer name="config_deepShortcutOpenDuration">220</integer>
    <integer name="config_deepShortcutArrowOpenDuration">80</integer>
    <integer name="config_deepShortcutOpenStagger">40</integer>
    <integer name="config_deepShortcutCloseDuration">150</integer>
    <integer name="config_deepShortcutCloseStagger">20</integer>
    <integer name="config_removeNotificationViewDuration">300</integer>
    
    <item type="id" name="view_scrim" />
    <string name="color_extraction_impl_class"></string>
    <integer name="extracted_color_gradient_alpha">153</integer>
</resources>
