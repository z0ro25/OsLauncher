<?xml version="1.0" encoding="utf-8"?>
<!-- Dòng app dạng DỌC (icon trái + tên phải) dùng cho list gợi ý dọc / kết quả tìm kiếm. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:background="@drawable/item_apps_library_search_view_state"
    android:focusable="true"
    android:clickable="true"
    android:paddingStart="6dp"
    android:paddingEnd="12dp"
    android:paddingTop="6dp"
    android:paddingBottom="6dp"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <com.amz.ios.launcher.FullBubbleTextView
        android:id="@+id/icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:scaleX="0.9"
        android:scaleY="0.9"
        app:hideIconName="true" />

    <com.amz.ios.launcher.TextViewCustomFont
        android:id="@+id/text"
        android:textSize="17sp"
        android:textColor="@color/white"
        android:gravity="center_vertical"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        app:customFont="1" />
</LinearLayout>
