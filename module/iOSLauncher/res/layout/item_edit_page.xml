<?xml version="1.0" encoding="utf-8"?>
<!-- Một page trong màn Edit Pages: thumbnail render thật (bo góc) + dấu tích ẩn/hiện phía dưới.
     Kích thước item được set động trong adapter (theo tỉ lệ màn hình / 2 hàng). -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/item_root"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:gravity="center_horizontal"
    android:orientation="vertical"
    android:paddingStart="8dp"
    android:paddingTop="8dp"
    android:paddingEnd="8dp"
    android:paddingBottom="8dp">

    <androidx.cardview.widget.CardView
        android:id="@+id/card_thumb"
        android:layout_width="100dp"
        android:layout_height="180dp"
        android:elevation="0dp"
        app:cardBackgroundColor="#22FFFFFF"
        app:cardCornerRadius="16dp"
        app:cardElevation="0dp">

        <ImageView
            android:id="@+id/iv_thumb"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="fitCenter" />
    </androidx.cardview.widget.CardView>

    <ImageView
        android:id="@+id/iv_check"
        android:layout_width="26dp"
        android:layout_height="26dp"
        android:layout_marginTop="10dp"
        android:src="@drawable/ic_page_check_on" />

</LinearLayout>
