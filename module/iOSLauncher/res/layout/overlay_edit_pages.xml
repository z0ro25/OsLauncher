<?xml version="1.0" encoding="utf-8"?>
<!-- Overlay toàn màn hình cho màn "Edit Pages" (kiểu iOS).
     Nền scrim tối mờ lộ wallpaper phía sau; nút Done góc phải-trên; các thumbnail page
     xếp thành 2 hàng, cuộn ngang bằng BouncyRecyclerView (kéo-thả đổi thứ tự). -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#CC000000"
    android:clickable="true"
    android:focusable="true">

    <TextView
        android:id="@+id/btn_done"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end|top"
        android:layout_marginTop="12dp"
        android:layout_marginEnd="16dp"
        android:background="?android:attr/selectableItemBackgroundBorderless"
        android:padding="8dp"
        android:text="@string/done"
        android:textColor="#FFFFFF"
        android:textSize="17sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/tv_edit_pages_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal|top"
        android:layout_marginTop="14dp"
        android:text="@string/edit_pages_title"
        android:textColor="#FFFFFF"
        android:textSize="17sp"
        android:textStyle="bold" />

    <com.amz.ios.launcher.bounce.BouncyRecyclerView
        android:id="@+id/rcv_pages"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:clipToPadding="false"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:overScrollMode="never"
        app:allow_drag_reorder="true" />

</FrameLayout>
