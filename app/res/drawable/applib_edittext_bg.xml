<?xml version="1.0" encoding="utf-8"?>
<!-- Nền ô nhập tên folder (dialog New Folder): nền onb_bg + viền onb_divider, bo 12dp (2-theme). RIÊNG. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/onb_bg" />
    <corners android:radius="12dp" />
    <stroke
        android:width="1dp"
        android:color="@color/onb_divider" />
</shape>
