<?xml version="1.0" encoding="utf-8"?>
<!-- Nút "Cancel" dialog New Folder: pill xám bo 14dp, nền onb_bg + viền onb_divider (2-theme). RIÊNG. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="14dp" />
    <solid android:color="@color/onb_bg" />
    <stroke
        android:width="1dp"
        android:color="@color/onb_divider" />
</shape>
